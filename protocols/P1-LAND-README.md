# P1-LAND-READY — mirror for GitHub MCP push
stamp: 2026-09-23 ~19:07 AEST

Ready-to-paste file set mirroring deep-research P1 protocol + schema atoms.
**Commander pushes later via GitHub MCP. This folder was NOT pushed by the executor.**

Suggested HE landing paths (owner/commander decision):

| Local mirror | Suggested repo path |
|---|---|
| `protocols/P1-*.md` + header | `protocols/p1/` or `CROSS-SCALE/p1/` |
| `schemas/IS-0001.yaml`…`IS-0008.yaml` | `schemas/` (IS-0009 already on main — do not overwrite) |
| `schemas/IS-0009.POINTER.md` | optional docs-only; skip if redundant |

Source of truth on disk: `../DEEP-RESEARCH-20260923/`.
